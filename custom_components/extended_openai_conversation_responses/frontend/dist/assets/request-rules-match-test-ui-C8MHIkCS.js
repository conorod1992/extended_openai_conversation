var e=e=>({equals:`Equals`,starts_with:`Starts with`,ends_with:`Ends with`,contains:`Contains`,sentence_pattern:`Sentence pattern`})[e]||e,t=2048;function n(e){let t=Number(e?.would_do?.action_count||0),n=(e?.would_do?.functions||[]).map(e=>`${e.name}${e.result_alias?` → ${e.result_alias}`:``}`).join(`, `);return`Would run ${t} local action${t===1?``:`s`}${n?`, including ${n}`:``}. ${e?.would_do?.consumed?`The command would be consumed locally.`:`After success, the original request would continue to the AI.`} Nothing was executed and no Function result was invented.`}function r(e){let t=e?.would_do||{},n=!!t.consumed,r=t.scope===`conversation`;if(t.reset)return n?`Would consume this command locally and return the rest of this conversation to the configured model settings. The AI provider would not receive this command. Nothing was changed.`:r?`Would clear the conversation routing override, then send the original request unchanged to the AI provider using the configured model settings. Nothing was changed in this preview.`:`Would send the original request unchanged to the AI provider using configured model settings for this request only. The saved conversation routing override would remain for the next request. Nothing was changed in this preview.`;let i=[];t.model&&i.push(`model ${t.model}`),t.reasoning_effort&&i.push(`${t.reasoning_effort} reasoning`);let a=i.join(` and `)||`the routing override`;return n?`Would consume this command locally and apply ${a} to the rest of this conversation. The AI provider would not receive this command. Nothing was changed.`:`Would send the original request unchanged to the AI provider using ${a} for ${r?`this request and later requests in this conversation`:`this request only`}. Nothing was changed in this preview.`}function i(t,i){let a=(i?.skipped_conditions||[]).map(e=>e.name).join(`, `);if(!i?.matched)return a?`<div class="notice"><strong>No eligible Request Rule</strong><p>Text matched ${t._e(a)}, but Only when conditions were false. No action executed.</p></div>`:`<div class="notice"><strong>No Request Rule matched</strong><p>In a real request, processing would continue normally and the original request could reach the AI provider. This preview did not run a Home Assistant action or call the provider.</p></div>`;let o=i.rule||{},s=Object.entries(i.captured_values||{}),c=i.fuzzy?`Fuzzy match · ${Number(i.score||0).toFixed(1)}%`:`Normal match`,l=o.action_type===`local_action`?`Local command`:`AI routing`,u=o.action_type===`local_action`?n(i):r(i),d=(i.matched_rules||[]).map(e=>`<li>${t._e(e.rule.name)} — ${e.status===`would_send_to_ai`?`would send to AI, stopped`:e.status}</li>`).join(``);return`<div class="notice on rule-match-preview"><strong>Matched: ${t._e(o.name||`Unnamed rule`)}</strong>${d?`<ol class="rule-match-chain">${d}</ol>`:``}<p>${t._e(l)} · ${t._e(e(o.match_type))} · ${t._e(c)}</p><dl class="match-preview-details"><div><dt>Matched phrase</dt><dd>${t._e(i.matched_phrase||`—`)}</dd></div>${s.length?`<div><dt>Captured values</dt><dd>${s.map(([e,n])=>`${t._e(e)} → ${t._e(n)}`).join(`<br>`)}</dd></div>`:``}${a?`<div><dt>Skipped</dt><dd>${t._e(a)}: Only when conditions were false</dd></div>`:``}<div><dt>Would happen</dt><dd>${t._e(u)}</dd></div></dl></div>`}function a(){return`<section class="content-card" id="rule-match-tester"><h2>Preview rule match (safe)</h2><p>Checks which enabled Request Rule would win using the real matcher, without running the resulting action.</p><div class="notice on"><strong>Safe preview — nothing executes</strong><p>No Home Assistant action runs, conversation routing is not changed, and the AI provider is not called.</p></div><div class="search-row"><input id="rule-match-test-text" type="text" maxlength="${t}" placeholder="Turn off the kitchen light" aria-label="Request text to test against Request Rules"><button type="button" id="rule-match-test">Preview match</button></div><p class="help">Preview and live Request Rule matching inspect at most ${t} characters (and 256 words).</p><div id="rule-match-test-result" aria-live="polite"></div></section>`}function o(e){let t=e.shadowRoot,n=t.querySelector(`#rule-match-test-text`),r=t.querySelector(`#rule-match-test`),a=t.querySelector(`#rule-match-test-result`);if(!n||!r||!a)return;let o=async()=>{let t=n.value;if(t.trim()&&!r.disabled){r.disabled=!0,a.textContent=`Checking…`;try{let n=await e._call(`request_rules`,`test_match`,{text:t});a.innerHTML=i(e,n)}catch(e){a.textContent=e.message||String(e)}finally{r.disabled=!1}}};r.addEventListener(`click`,()=>{o()}),n.addEventListener(`keydown`,e=>{e.key===`Enter`&&(e.preventDefault(),o())})}export{t as REQUEST_RULE_MATCH_MAX_CHARS,o as bindRequestRuleMatchTester,i as formatRequestRuleMatchResult,a as renderRequestRuleMatchTester};